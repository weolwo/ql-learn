<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alex.dao.ExpressMapper">
    <resultMap type="com.alex.bean.Express" id="ExpressResult">
        <id property="id" column="id"/>
        <result property="express" column="express"/>
        <result property="activityCode" column="activity_code"/>
    </resultMap>

    <sql id="selectConfigVo">
        select id, express, activity_code from t_express
    </sql>
    <select id="selectExpressById" resultMap="ExpressResult">
        <include refid="selectConfigVo"/> where id=#{id}
    </select>

</mapper>